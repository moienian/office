import React from "react";
import ReactDOM from "react-dom";

const template = <p>chetori?</p>;
// const template = React.createElement("p", {}, "salam");
const root = document.getElementById("root");

ReactDOM.render(template, root);
